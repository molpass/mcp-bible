# mcp-bible v1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Hermes에 성경 도우미 MCP를 추가한다 — 다역본·원어·렉시콘 구절 조회(점진 공개), keyword/semantic 검색, 관주, 단어연구, 그리고 목회적 톤의 행동 판단(근거 구절 + 공감)을 SKILL로 묶는다.

**Architecture:** MCP(손발, 결정론 6도구) / SKILL(지식, 웨슬리안 사변형 해석틀) 분리. 모든 자료를 공통 OSIS 절-ID(`GEN.1.1`)로 정규화해 단일 `node:sqlite` DB(FTS5 포함) + 역본별 BGE-M3 임베딩 `.bin`으로 빌드. 내부는 corpus-loader / verse-id / search / skill-frame 4분할로 멀티종교(향후 sutra·quran) 골격 재사용.

**Tech Stack:** Node 22 / TypeScript(ESM, Node16 모듈) · `@modelcontextprotocol/sdk` ^1.12 · `zod` ^3.23 · **`node:sqlite`(빌트인, FTS5, 무네이티브빌드)** · BGE-M3 임베딩(DeepInfra OpenAI 호환 `/embeddings`, `DEEPINFRA_TOKEN`) · 검증 `node --test`(빌트인).

---

## 확정 결정 (R1 캐치백 — 2026-06-21 PM 결정문)

1. **빌드 산출물 배포** — **임베딩 `.bin`만** 1회 빌드 → **GitHub Release 자산**으로 배포(배포 박스는 자산 다운로드·배치, 박스마다 재생성 금지 = DeepInfra 비용 N배·오프라인 우선 위배 회피). **퍼블릭 도메인 텍스트 DB `bible.sqlite`(수십 MB)는 레포에 커밋** → 공개 클론자가 토큰 없이 lookup·keyword 즉시 동작. **개역개정 로컬 본문은 어느 경로로도 git 진입 금지**(불변).
   - `.gitignore`: `data/sources/`·`data/embeddings/`·`data/local/*.json` 제외. **`data/bible.sqlite`는 커밋(제외 안 함)**.

2. **semantic 임베딩 코퍼스 = 텍스트 3역본만**(개역한글·BSB·KJV). 히/헬 **원어는 임베딩 제외**, lookup·word_study 전용으로 본문·형태소·렉시콘 전부 보존(잘리는 건 죽은 인덱스 하나). 향후 "특정 원어 단어 출현 구절"은 임베딩 아니라 **Strong's 번호 인덱스(결정론)** 로 — word_study 데이터에 이미 존재. → `versions.embedded=1`은 krv·bsb·kjv만.
   - semantic은 질의당 DeepInfra 1회(§7) → 오프라인 아님. §9 "오프라인 우선"은 lookup·keyword. 토큰 부재 시 semantic→keyword 폴백+안내.

3. **데이터 엔진 = `node:sqlite`**(빌트인·무빌드·FTS5·3박스 이식성, experimental 경고 허용). 전제: ① **전 박스 Node 22.x 핀**(experimental API 변동 방지) ② **DB 접근은 `src/db.ts` 단일 모듈 뒤로 격리** → 필요 시 better-sqlite3로 한 모듈 교체만으로 전환(가역성).

4. **PM 손** — ① `molpass/mcp-bible` 공개 레포 생성(MIT) ② `DEEPINFRA_TOKEN` env 주입(임베딩 빌드/런타임 전제, env 전용·계획/레포 미진입). `build:embeddings`는 토큰 설정 박스에서만 실행.

### 신규 게이트 2건 (필수)

- **G1 — 교차 소스 절-ID 정렬 검증** (Task 8b, build-db/임베딩 직전): "행수 검증"과 별개로 개역한글 versification ↔ OpenBible 관주 ↔ OSHB(히) ↔ Berean(헬)이 **동일 OSIS ID로 1:1 매핑**되는지 확인. 불일치 ID 해소 후 다음 진입. *사유: 어긋난 ID에 임베딩 돌리면 비용 낭비 + lookup 다역본·관주·word_study·회수 품질이 전부 미묘하게 깨짐 — 가장 비싸게 틀리고 되돌리기 어려운 지점.*
- **G2 — Phase 1 후 PM 체크포인트**: 정규화 완료 + G1 통과 + build-db 직후, **임베딩 빌드(DeepInfra 지출) 이전**에 PM 검수 1회. 통과해야 Phase 2(임베딩·도구) 진입.

### 실행 = subagent-driven fan-out (3조건부)

소스별 fetch→포맷검사→정규화 순서 엄수(미검사 포맷 추측 금지) · 임베딩 이전 G1 통과 · Phase 1 후 G2 통과. 세 조건 모두 본 계획에 반영됨.

---

## File Structure

공개 레포 `molpass/mcp-bible` (PM이 GitHub에 생성 — 현재 미존재). 로컬 빌드 위치 `C:\Users\zeone\work\mcp-bible`.

```
mcp-bible/
├─ package.json                    # ESM, deps, scripts(build, build:data, test, example)
├─ tsconfig.json                   # Node16/ES2022 strict (mcp-qr 동일)
├─ .gitignore                      # node_modules/ dist/ dist-scripts/ data/sources/ data/embeddings/ data/local/*.json *.token  (※ data/bible.sqlite 는 커밋 — 결정 1)
├─ .gitattributes                  # text=auto eol=lf
├─ LICENSE                         # MIT (코드)
├─ NOTICE                          # 자료별 출처·라이선스·표기 의무 (§5 전체)
├─ README.md                       # 도구표 + 설치 + build:data + Hermes 등록 + About + 같은모음
├─ STANDARD.md                     # mcp-qr에서 복사
├─ src/
│  ├─ index.ts                     # MCP 서버(stdio) — 6 도구 등록
│  ├─ db.ts                        # node:sqlite open + 쿼리 헬퍼 (읽기 전용)
│  ├─ verse-id.ts                  # OSIS 절-ID: 66책 USFM 코드표 + ko/en 별칭 + 참조 파서/포맷터
│  ├─ corpus-loader/
│  │  ├─ types.ts                  # Version, Book, Verse, OriginalToken, Lexeme, XRef 인터페이스
│  │  └─ registry.ts              # 번들 버전 + data/local/*.json 스캔 병합 + 기본역본 결정
│  ├─ search/
│  │  ├─ keyword.ts                # FTS5 퍼지 검색
│  │  └─ semantic.ts              # 질의 임베딩(DeepInfra) + .bin brute-force 코사인 + keyword 폴백
│  ├─ embeddings.ts                # BGE-M3 클라이언트(배치) — build + query 공용
│  └─ tools/
│     ├─ lookup.ts
│     ├─ search.ts
│     ├─ cross_references.ts
│     ├─ word_study.ts
│     ├─ list_versions.ts
│     └─ list_books.ts
├─ scripts/                        # 빌드 파이프라인(공개 — 스크립트는 공개, 받은 원본은 gitignore)
│  ├─ fetch-sources.ts             # §5 소스 전부 data/sources/ 로 다운로드
│  ├─ normalize/                   # 소스별 정규화 → 공통 스키마 행
│  │  ├─ krv.ts                    # 개역한글
│  │  ├─ bsb.ts                    # Berean Standard Bible
│  │  ├─ kjv.ts                    # KJV
│  │  ├─ wlc.ts                    # 히브리어 OSHB/WLC + morphhb 형태소
│  │  ├─ berean-greek.ts           # 헬라어 Berean + interlinear
│  │  ├─ byzantine.ts              # (옵션) 비잔틴 다수본문
│  │  ├─ lexicon.ts                # Strong's 히/헬 렉시콘
│  │  └─ cross-refs.ts             # OpenBible 관주
│  ├─ build-db.ts                  # 정규화 행 → bible.sqlite + FTS5
│  └─ build-embeddings.ts          # 텍스트 역본 → data/embeddings/<vid>.bin + .idx.json
├─ data/
│  ├─ sources/                     # 원본 다운로드 (gitignore)
│  ├─ bible.sqlite                 # 빌드 산출물 (gitignore)
│  ├─ embeddings/                  # <vid>.bin + <vid>.idx.json (gitignore)
│  └─ local/
│     ├─ README.md                 # 로컬 역본 포맷 설명(개역개정 투입 가이드)
│     └─ .gitkeep
├─ skill/
│  └─ bible.skill.md               # 트리거 성경/기독교 + 사변형틀 + 호출규약
├─ examples/
│  └─ run-examples.mjs             # 6도구 1회씩 호출 → 출력 확인(스모크)
├─ test/
│  ├─ verse-id.test.js
│  ├─ registry.test.js
│  ├─ tools.test.js                # DB 픽스처 기반 6도구 계약
│  └─ skill-contract.test.js       # 호출규약(단일 synthesis) 정적 점검
└─ docs/plans/2026-06-21-mcp-bible-v1.md   # 이 문서
```

**책임 경계**: `src/`는 읽기 전용 런타임(DB 조회 + 검색 + 도구). `scripts/`는 빌드 전용(쓰기). 둘은 `corpus-loader/types.ts` 스키마를 공유하지만 런타임은 scripts를 import하지 않는다.

---

## 공통 계약 (모든 태스크가 이 인터페이스를 타깃)

### 절-ID (verse-id)
- USFM 3글자 대문자 책코드 66종: `GEN EXO LEV NUM DEU … MAT MRK LUK JHN … REV`.
- 절-ID 형식: `<BOOK>.<chapter>.<verse>` (예 `GEN.1.1`, `JHN.3.16`).
- 참조 파서: `parseRef(s): {start: VerseId, end?: VerseId} | null` — 지원: `"John 3:16"`, `"John 3:16-18"`, `"요 3:16"`, `"요한복음 3장 16절"`, `"GEN.1.1"`. 책 별칭표(en full/abbr + ko full/abbr → USFM).

### SQLite 스키마 (build-db.ts가 생성, src가 읽음)
```sql
CREATE TABLE versions(version_id TEXT PRIMARY KEY, name TEXT, lang TEXT, license TEXT,
  source TEXT DEFAULT 'bundled', direction TEXT DEFAULT 'ltr', has_original INTEGER DEFAULT 0, embedded INTEGER DEFAULT 0);
CREATE TABLE books(book_id TEXT PRIMARY KEY, ord INTEGER, name_en TEXT, name_ko TEXT, testament TEXT, chapters INTEGER);
CREATE TABLE verses(version_id TEXT, verse_id TEXT, book_id TEXT, chapter INTEGER, verse INTEGER, text TEXT,
  PRIMARY KEY(version_id, verse_id));
CREATE INDEX idx_verses_ref ON verses(version_id, book_id, chapter, verse);
CREATE VIRTUAL TABLE verses_fts USING fts5(verse_id UNINDEXED, version_id UNINDEXED, text,
  tokenize='unicode61 remove_diacritics 2');
CREATE TABLE original_tokens(verse_id TEXT, lang TEXT, position INTEGER, surface TEXT, lemma TEXT,
  strongs TEXT, morph TEXT, PRIMARY KEY(verse_id, lang, position));
CREATE INDEX idx_tokens_strongs ON original_tokens(strongs);
CREATE TABLE lexicon(strongs TEXT PRIMARY KEY, lang TEXT, lemma TEXT, translit TEXT, gloss TEXT, definition TEXT);
CREATE TABLE cross_refs(from_verse TEXT, to_start TEXT, to_end TEXT, votes INTEGER DEFAULT 0,
  PRIMARY KEY(from_verse, to_start, to_end));
CREATE INDEX idx_xref_from ON cross_refs(from_verse, votes DESC);
```

### 임베딩 파일
- `data/embeddings/<version_id>.bin`: float32 LE, `[N × 1024]` 연속(L2 정규화 저장).
- `data/embeddings/<version_id>.idx.json`: `string[]` (행 순서 = verse_id).

### 도구 출력
- 모두 `{ content: [{ type: "text", text: <구조화 텍스트> }] }` (v1 PNG 없음, 발주서 §7).
- 역본 본문 표기 의무: 각 구절 끝/머리에 역본명 표기(예 `(개역한글)`).

---

## Phase 0 — 스캐폴드

### Task 1: 레포 스캐폴드 (mcp-qr 복제 기반)

**Files:**
- Create: `package.json`, `tsconfig.json`, `.gitignore`, `.gitattributes`, `LICENSE`, `STANDARD.md`
- Reference: `C:\Users\zeone\work\_ref-mcp-qr\` (정찰용 클론, 복사 출처)

- [ ] **Step 1: 디렉토리 + 정적 파일 복사**

`STANDARD.md`·`.gitattributes`·`LICENSE`는 `_ref-mcp-qr`에서 복사. `tsconfig.json`은 mcp-qr와 동일.

- [ ] **Step 2: package.json 작성**

```json
{
  "name": "mcp-bible",
  "version": "1.0.0",
  "description": "Bible helper MCP — multi-version lookup, original-language word study, keyword/semantic search, cross-references. stdio.",
  "type": "module",
  "bin": { "mcp-bible": "dist/index.js" },
  "files": ["dist", "data/bible.sqlite"],
  "scripts": {
    "build": "tsc",
    "build:scripts": "tsc -p tsconfig.scripts.json",
    "fetch": "npm run build:scripts && node dist-scripts/fetch-sources.js",
    "build:db": "npm run build:scripts && node dist-scripts/build-db.js",
    "build:embeddings": "npm run build:scripts && node dist-scripts/build-embeddings.js",
    "start": "node dist/index.js",
    "example": "node examples/run-examples.mjs",
    "test": "node --test"
  },
  "keywords": ["mcp","model-context-protocol","hermes-agent","claude","bible","korean","telegram"],
  "license": "MIT",
  "engines": { "node": "22.x" },
  "dependencies": { "@modelcontextprotocol/sdk": "^1.12.0", "zod": "^3.23.8" },
  "devDependencies": { "@types/node": "^22.0.0", "typescript": "^5.5.0" }
}
```

> 결정 1·3 반영: `files`에 `data/bible.sqlite`만(임베딩=Release 자산, git 미포함). `engines.node="22.x"` 핀(node:sqlite experimental 안정). scripts(.ts)는 `tsconfig.scripts.json`(rootDir scripts, outDir dist-scripts)로 별도 컴파일. `build:embeddings`는 DEEPINFRA_TOKEN 설정 박스에서만.

- [ ] **Step 3: 빌드 확인** — Run: `npm install && npm run build` → Expected: `dist/` 생성(아직 src 없음 → 빈 빌드 OK 또는 index.ts 추가 후).

- [ ] **Step 4: Commit** — `chore: scaffold mcp-bible from mcp-qr standard`

---

### Task 2: verse-id (절-ID 파서) — TDD

**Files:** Create `src/verse-id.ts` · Test `test/verse-id.test.js`

- [ ] **Step 1: 실패 테스트**

```js
import { test } from "node:test";
import assert from "node:assert/strict";
import { parseRef, formatRef, BOOKS } from "../dist/verse-id.js";

test("66 books", () => assert.equal(BOOKS.length, 66));
test("osis passthrough", () => assert.deepEqual(parseRef("GEN.1.1"), { start: "GEN.1.1" }));
test("en ref", () => assert.deepEqual(parseRef("John 3:16"), { start: "JHN.3.16" }));
test("en range", () => assert.deepEqual(parseRef("John 3:16-18"), { start: "JHN.3.16", end: "JHN.3.18" }));
test("ko abbr", () => assert.deepEqual(parseRef("요 3:16"), { start: "JHN.3.16" }));
test("ko full", () => assert.deepEqual(parseRef("요한복음 3장 16절"), { start: "JHN.3.16" }));
test("invalid", () => assert.equal(parseRef("zzz"), null));
```

- [ ] **Step 2: 실패 확인** — `npm run build && node --test test/verse-id.test.js` → FAIL (module/export 없음)

- [ ] **Step 3: 구현** — `src/verse-id.ts`에 66책 테이블(`{id, ord, en, enAbbr[], ko, koAbbr[], testament, chapters}`) + 별칭 역인덱스 + `parseRef`(OSIS 정규식 우선 → en/ko "책 장:절" / "책 장장 절절" 패턴 → 범위 `-`) + `formatRef`. 한국어 책명 66종 + 통용 약칭(창/출/레…/마/막/눅/요…/계) 포함.

- [ ] **Step 4: 통과 확인** — `npm run build && node --test test/verse-id.test.js` → PASS

- [ ] **Step 5: Commit** — `feat: verse-id OSIS parser with ko/en book aliases`

---

### Task 3: corpus-loader 타입 + 레지스트리 — TDD

**Files:** Create `src/corpus-loader/types.ts`, `src/corpus-loader/registry.ts` · Test `test/registry.test.js`

- [ ] **Step 1: 실패 테스트** — 임시 sqlite 픽스처(versions 2행: `krv` bundled + 로컬 스캔 mock)로:

```js
import { test } from "node:test";
import assert from "node:assert/strict";
import { listVersions, defaultVersion } from "../dist/corpus-loader/registry.js";
// 픽스처 DB 경로/로컬 디렉토리 주입형으로 설계
test("bundled listed", () => { const v = listVersions(FIXTURE); assert.ok(v.find(x=>x.version_id==="krv")); });
test("local flag", () => { const v = listVersions(FIXTURE_WITH_LOCAL); assert.equal(v.find(x=>x.source==="local").version_id, "krv-rev"); });
test("default = local krv-rev when present", () => assert.equal(defaultVersion(FIXTURE_WITH_LOCAL), "krv-rev"));
test("default = krv when no local", () => assert.equal(defaultVersion(FIXTURE), "krv"));
```

- [ ] **Step 2: 실패 확인**
- [ ] **Step 3: 구현** — `types.ts`(Version/Book/Verse/OriginalToken/Lexeme/XRef). `registry.ts`: `listVersions(db, localDir)` = DB versions + `data/local/*.json` 스캔(version_id/version_name 읽어 `source:"local"` 병합). `defaultVersion` = 로컬 개역개정 존재 시 그것, 없으면 `krv`(개역한글).
- [ ] **Step 4: 통과 확인**
- [ ] **Step 5: Commit** — `feat: version registry with local-module scan + default resolution`

---

## Phase 1 — 데이터 파이프라인 (PM: 전 소스 일괄)

> 각 정규화 태스크는 **fetch → 실제 포맷 검사 → 공통 스키마로 정규화 → 행수 검증** 순. 파서 본문은 실행 시 검사한 실제 포맷에 맞춘다(미검사 포맷 추측 금지 — karpathy §1). 타깃 스키마는 위 "공통 계약" 고정.

### Task 4: fetch-sources

**Files:** Create `scripts/fetch-sources.ts`

- [ ] **Step 1: 구현** — §5 소스를 `data/sources/<key>/`로 다운로드:
  - krv: `getbible`/`seven1m/open-bibles` 개역한글
  - bsb: bereanbible.com (berean.bible CC0 텍스트/인터리니어)
  - kjv: `seven1m/open-bibles` 또는 `scrollmapper`
  - wlc: `openscriptures/morphhb` (WLC + 형태소 XML)
  - berean-greek: bereanbible.com Greek + interlinear
  - byzantine(옵션): `byztxt/byzantine-majority-text`
  - lexicon: `morphgnt/strongs-dictionary-xml`(헬, CC0) + `openscriptures/HebrewLexicon`(CC BY) + `openscriptures/strongs`
  - xrefs: `openbible.info` cross_references.txt + `scrollmapper/bible_databases`
  - GitHub 소스는 `git clone --depth 1` 또는 raw fetch. 웹사이트는 공개 다운로드 URL.
- [ ] **Step 2: 검증** — `node ... fetch-sources.ts` 후 각 `data/sources/<key>/`에 파일 존재 + 비어있지 않음(바이트>0) 출력. **각 소스 실제 포맷(첫 200줄/구조)을 로그로 남겨 정규화 태스크 입력으로 사용.**
- [ ] **Step 3: Commit** — `feat: source fetcher for all §5 datasets` (받은 원본은 gitignore)

### Task 5: 정규화 — 한국어 개역한글 (krv) — TDD

**Files:** Create `scripts/normalize/krv.ts` · Test `test/normalize-krv.test.js`

- [ ] **Step 1: 포맷 검사** — `data/sources/krv/` 실제 구조 확인(JSON? USFM?). 결과를 주석에 기록.
- [ ] **Step 2: 실패 테스트** — `normalizeKrv(srcDir)` → `Verse[]` (verse_id `GEN.1.1`, text 비어있지 않음). 픽스처 소량으로 `GEN.1.1`·`JHN.3.16` 존재 + 총 절수 ≈ 31,100(±오차) assert.
- [ ] **Step 3: 실패 확인**
- [ ] **Step 4: 구현** — 검사한 실제 포맷 → 공통 `Verse[]`. 본문 변형 금지(대한성서공회 표기, PD-인격권). 책 코드 매핑은 verse-id 별칭표 재사용.
- [ ] **Step 5: 통과 확인** (총 절수 + 샘플 절 존재)
- [ ] **Step 6: Commit** — `feat: normalize KRV(개역한글) to common verse-id`

### Task 6: 정규화 — BSB / KJV — TDD
**Files:** Create `scripts/normalize/bsb.ts`, `scripts/normalize/kjv.ts` · Test `test/normalize-en.test.js`
- [ ] Task 5 패턴 반복(소스별 포맷 검사 → 정규화 → 절수/샘플 검증). BSB CC0, KJV PD. **Commit** each: `feat: normalize BSB`, `feat: normalize KJV`.

### Task 7: 정규화 — 원어(WLC 히브리어 + Berean 헬라어) + 형태소/Strong's — TDD
**Files:** Create `scripts/normalize/wlc.ts`, `scripts/normalize/berean-greek.ts` · Test `test/normalize-original.test.js`
- [ ] **Step 1: 포맷 검사** — morphhb XML 구조(w lemma/morph), Berean interlinear 정렬.
- [ ] **Step 2: 실패 테스트** — `normalizeWlc` → `{verses: Verse[], tokens: OriginalToken[]}`; `GEN.1.1` 첫 토큰 surface=`בְּרֵאשִׁית` lemma/strongs 존재(`H7225`). 헬라어 `JHN.1.1` 토큰 strongs(`G3056` 등).
- [ ] **Step 3~5: 구현 → 통과** — verses(원어 본문) + original_tokens(position/surface/lemma/strongs/morph). 책코드 매핑.
- [ ] **Step 6: Commit** — `feat: normalize Hebrew(WLC)+Greek(Berean) with morphology + Strong's tags`

### Task 7b (옵션): 비잔틴 다수본문 — Task 6 패턴. PM이 옵션 모듈로만 원하면 `versions.embedded=0`. Commit `feat: normalize Byzantine majority (optional module)`.

### Task 8: 정규화 — 렉시콘 + 관주 — TDD
**Files:** Create `scripts/normalize/lexicon.ts`, `scripts/normalize/cross-refs.ts` · Test `test/normalize-lex-xref.test.js`
- [ ] **lexicon**: 히(openscriptures/HebrewLexicon CC BY) + 헬(morphgnt strongs-dictionary CC0) → `Lexeme[]`(strongs/lang/lemma/translit/gloss/definition). 검증: `H7225`·`G3056` gloss 존재.
- [ ] **cross-refs**: OpenBible cross_references.txt → `XRef[]`(from_verse/to_start/to_end/votes). 검증: `GEN.1.1` from 행 다수 + votes 정렬.
- [ ] **Commit** — `feat: normalize Strong's lexicon (he/grc) + OpenBible cross-references`

### Task 8b: G1 — 교차 소스 절-ID 정렬 게이트 (필수, build-db 직전) — TDD

**Files:** Create `scripts/verify-alignment.ts` · Test `test/alignment.test.js`

- [ ] **Step 1: 실패 테스트** — `verifyAlignment(normalized): { ok: boolean, mismatches: {kind, verse_id, sources}[] }`. 정규화 결과(krv·bsb·kjv·wlc·berean·xref) 입력 → (a) 관주 from/to 절-ID가 어느 텍스트 역본에도 없으면 mismatch (b) 원어 토큰 verse_id가 텍스트 절-ID에 없으면 mismatch (c) 책별 장/절 카운트가 역본 간 정합(±허용). 픽스처로 의도적 불일치 1건 → `ok:false`.
- [ ] **Step 2: 실패 확인**
- [ ] **Step 3: 구현** — 공통 절-ID 셋 교차 검증 + 알려진 versification 차이(예: 시편 표제, 말라기/요엘 장 분할)는 매핑표로 해소, 미해소는 mismatch 보고. `ok:false`면 **build-db 진입 금지**(스크립트 exit 1).
- [ ] **Step 4: 통과 확인** — 실데이터에서 `ok:true` 또는 mismatch 목록 해소 후 통과.
- [ ] **Step 5: Commit** — `feat: G1 cross-source verse-id alignment gate`

> ⚠️ 어긋난 ID에 임베딩 돌리면 비용 낭비 + lookup/관주/word_study/회수 품질 동시 손상. 이 게이트 PASS 전 절대 임베딩 진입 금지.

### Task 9: build-db — 단일 SQLite 조립 + FTS5 — TDD
**Files:** Create `scripts/build-db.ts` · Test `test/build-db.test.js`
- [ ] **Step 1: 실패 테스트** — 소량 정규화 행 입력 → `data/bible.sqlite` 생성 → versions/books/verses/original_tokens/lexicon/cross_refs 행수 + `verses_fts` 매치(`SELECT … verses_fts WHERE text MATCH '사랑'`) 검증.
- [ ] **Step 2~4: 구현 → 통과** — `node:sqlite DatabaseSync`로 스키마 생성(위 DDL) → 정규화 모듈 호출 결과 일괄 insert(트랜잭션) → FTS5 채움 → `versions.embedded` 플래그(텍스트 역본만 1). scripts TS 컴파일 방식 확정(`tsc -p tsconfig.scripts.json` → `dist-scripts/`).
- [ ] **Step 5: Commit** — `feat: assemble single bible.sqlite with FTS5`

---

## 🛑 G2 — PM 체크포인트 (Phase 1 종료, 임베딩 빌드 이전 · 필수 정지)

여기서 **반드시 멈추고 PM 검수**. 통과해야 Phase 2(임베딩·도구) 진입. *DeepInfra 지출은 이 게이트 통과 후에만.*

- [ ] 정규화 완료(krv·bsb·kjv·원어·렉시콘·관주) — 각 행수 검증 PASS
- [ ] **G1 정렬 게이트 PASS**(불일치 0 또는 해소 완료)
- [ ] `bible.sqlite` 생성 + FTS5 매치 동작
- [ ] PM에게 보고: 역본별 절수, 토큰/렉시콘/관주 건수, G1 결과, 샘플 lookup 1건 → **PM 승인 대기**

---

## Phase 2 — 임베딩 + MCP 도구 (G2 통과 후)

### Task 10: build-embeddings — BGE-M3 사전계산 (텍스트 3역본) — TDD
**Files:** Create `src/embeddings.ts`, `scripts/build-embeddings.ts` · Test `test/embeddings.test.js`
- [ ] **Step 1: embeddings.ts** — `embedBatch(texts: string[]): Promise<Float32Array[]>` → DeepInfra `POST https://api.deepinfra.com/v1/openai/embeddings` model `BAAI/bge-m3-multi`, `Authorization: Bearer ${DEEPINFRA_TOKEN}`, 배치(예 64), L2 정규화. 토큰 부재 시 throw(빌드) / null(런타임 폴백).
- [ ] **Step 2: 테스트(모킹)** — fetch 모킹으로 batch 분할·정규화(‖v‖≈1)·실패 재시도 단위 검증. 실호출 0.
- [ ] **Step 3: build-embeddings.ts** — `versions.embedded=1`인 역본의 모든 verse를 verse_id 순회 → `embedBatch` → `<vid>.bin`(연속 float32) + `<vid>.idx.json`. 진행 로그 + 재개(이미 생성분 skip).
- [ ] **Step 4: 검증** — 소수 역본·소수 절로 .bin 바이트수 == N×1024×4, idx 길이 == N.
- [ ] **Step 5: Commit** — `feat: BGE-M3 embedding precompute for text versions`

> ⚠️ 전체 실행은 **DEEPINFRA_TOKEN 필요** + 텍스트 3역본 × ~31,100절 ≈ 93k 임베딩(배치). 빌드 1회만 — **결과 `.bin`은 GitHub Release 자산으로 업로드(결정 1)**, 배포 박스는 자산 다운로드·배치(박스마다 재생성 금지).

---

### MCP 도구 (결정론 계약)

> 모든 도구는 읽기 전용 `db.ts`(node:sqlite 읽기 핸들) 사용. Test `test/tools.test.js`는 Task 9 소형 픽스처 DB로.

### Task 11: db.ts + list_books + list_versions — TDD
**Files:** Create `src/db.ts`, `src/tools/list_books.ts`, `src/tools/list_versions.ts`
- [ ] `db.ts`: `openDb(path)` 읽기 전용 + prepared 쿼리 헬퍼.
- [ ] `list_books()` → 66책(ord 정렬, name_en/ko, testament, chapters). `list_versions()` → registry.listVersions(번들+로컬, source 플래그).
- [ ] 테스트: 책 66 + 버전 목록 + 로컬 플래그. **Commit** `feat: db handle + list_books/list_versions tools`

### Task 12: lookup — TDD
**Files:** Create `src/tools/lookup.ts`
- [ ] **계약**: `lookup(reference, versions[]?, include_original=false, include_lexicon=false)`. reference→parseRef(범위 지원). versions 미지정 시 defaultVersion. 다역본 동시 반환(역본명 표기). include_original이면 original_tokens(surface/lemma/strongs/morph) 동봉, include_lexicon이면 토큰 strongs→lexicon gloss 조인.
- [ ] **테스트**: `JHN.3.16` 단/다역본, 범위 `JHN.3.16-18`, include_original(원어 토큰), include_lexicon(gloss). 미존재 절 → 빈/안내.
- [ ] **Commit** `feat: lookup tool (multi-version + original + lexicon, progressive)`

### Task 13: cross_references — TDD
**Files:** Create `src/tools/cross_references.ts`
- [ ] `cross_references(reference, limit=20)` → cross_refs from_verse 매치 votes 내림차순 limit, 각 to-구절 본문(defaultVersion) 동봉. 테스트: `GEN.1.1` 관주 반환 + 정렬 + limit.
- [ ] **Commit** `feat: cross_references tool (OpenBible)`

### Task 14: word_study — TDD
**Files:** Create `src/tools/word_study.ts`
- [ ] `word_study(reference, word?, strongs?)` — reference의 original_tokens에서 word(surface/lemma 매치) 또는 strongs로 토큰 특정 → strongs → lexicon(lemma/translit/gloss/definition) + 동일 strongs의 타 출현(빈도/대표 구절). 테스트: `JHN.1.1` word="λόγος" 또는 strongs="G3056" → gloss "word" 연결.
- [ ] **Commit** `feat: word_study tool (token→Strong's→lexicon)`

### Task 15: search (keyword FTS + semantic) — TDD
**Files:** Create `src/search/keyword.ts`, `src/search/semantic.ts`, `src/tools/search.ts`
- [ ] **keyword.ts**: FTS5 MATCH 퍼지(unicode61) → verse_id/version/snippet/score. 오프라인.
- [ ] **semantic.ts**: 질의 `embedBatch([query])` → 대상 역본 `.bin` mmap/read → brute-force 코사인 top-k. **DEEPINFRA_TOKEN 부재 시 keyword 폴백 + content에 "semantic 불가, keyword로 대체" 안내**.
- [ ] **search.ts**: `search(query, mode="keyword"|"semantic", limit=10)`. 테스트: keyword "사랑" 회수, semantic은 fetch 모킹(top-k 정렬) + 토큰부재 폴백 경로.
- [ ] **Commit** `feat: search tool (FTS keyword + BGE-M3 semantic with offline fallback)`

### Task 16: index.ts — 서버 + 6도구 등록 — 스모크
**Files:** Create `src/index.ts`, `examples/run-examples.mjs`
- [ ] `McpServer({name:"bible",version:"1.0.0"})` + 6 `registerTool`(zod inputSchema, 각 tools/* 핸들러 위임). stdio 연결.
- [ ] `examples/run-examples.mjs`: 픽스처/실DB로 6도구 1회씩 호출 출력(한글·히브리어·헬라어 유니코드 렌더 확인).
- [ ] 스모크 테스트(sajuMCP 패턴): stdio JSON-RPC `initialize`→`tools/list`(6개)→`tools/call lookup` 1왕복.
- [ ] **Commit** `feat: MCP server entry registering 6 tools (stdio)`

---

## Phase 3 — 로컬 모듈 · SKILL · 문서

### Task 17: 로컬 개역개정 모듈 (포맷·로더·격리)
**Files:** Create `data/local/README.md`, `data/local/.gitkeep`; verify `.gitignore`
- [ ] `data/local/README.md`: 포맷 명세 — `{ "version_id":"krv-rev","version_name":"개역개정","license_note":"private/local only","verses":{"GEN.1.1":"…"} }`. 공통 절-ID 키잉, 개역개정=개역한글 계보라 1:1, 미매칭 ID는 개역한글 폴백. **본문은 PM이 투입(발주 범위 밖)**.
- [ ] `.gitignore`에 `data/local/*.json` 확인(README/.gitkeep만 추적).
- [ ] registry(Task 3)가 로컬 스캔·기본역본 전환 이미 처리 → 통합 테스트: `data/local/krv-rev.json`(테스트 픽스처) 투입 시 list_versions 등장 + defaultVersion 전환, 제거 시 krv 폴백, **git status에 json 미노출** 확인.
- [ ] **Commit** `feat: local translation module (format + loader + git exclusion)`

### Task 18: SKILL — bible.skill.md
**Files:** Create `skill/bible.skill.md` · Test `test/skill-contract.test.js`
- [ ] frontmatter `name: bible`, `description:` (트리거 "성경"·"기독교", 구절 조회/행동 판단). 본문:
  - **해석틀**: 웨슬리안 사변형(성경·전통·이성·경험) — 감리교/열린 개신교 결.
  - **톤**: 공감·자율 존중, 강요·정죄·설교 회피. 행동 판단 = **근거 구절 먼저 + 공감 한마디**. 조회 = 묻고 점진 공개(역본→원어→어원→렉시콘).
  - **호출 규약(gemma 필수)**: `search` 1회(+필요 시 `cross_references` 1회)로 prefetch → **tool call 0으로 단일 synthesis**. 다중 체이닝 금지(작은 모델 붕괴 회피, memory `survivors`/`90_logic` gemma 패턴).
  - UX 예시 2종(발주서 §3) 포함.
- [ ] `skill-contract.test.js`: 정적 점검 — 파일에 "단일 synthesis"·"다중 체이닝 금지"·"근거 구절"·"사변형" 키워드 존재(규약 회귀).
- [ ] **Commit** `docs: bible SKILL (Wesleyan quadrilateral, pastoral tone, single-synthesis call rule)`

### Task 19: NOTICE + README
**Files:** Create `NOTICE`, `README.md`
- [ ] `NOTICE`: §5 자료별 출처·라이선스·표기 의무 — 개역한글(PD·인격권·대한성서공회 표기·본문 변형 금지), BSB(CC0 berean.bible), KJV(PD), OSHB/WLC(PD)+morphhb(CC BY), Berean Greek(CC0), Byzantine(PD/Unlicense), Strong's 헬(CC0)/히 HebrewLexicon(CC BY)+openscriptures/strongs, OpenBible xrefs(CC BY)+scrollmapper(MIT). **SBLGNT 번들 제외 명시**.
- [ ] `README.md`: 도구표(6) + 설치 + **`npm run build:data`(DEEPINFRA_TOKEN 필요·소스 다운로드·DB/임베딩 생성)** + Hermes 등록 JSON(서버명 `bible`, `node dist/index.js`, env `DEEPINFRA_TOKEN`) + 로컬 개역개정 안내 + About(molpass/Chat/Code) + 같은 모음 + License MIT. 설계노트(번들=빌드산출물·gitignore) 명시.
- [ ] **Commit** `docs: NOTICE (per-source licenses) + README (install/build/register)`

---

## Phase 4 — R3 검수 게이트 (발주서 §11, PASS/FAIL)

### Task 20: 전체 검수
- [ ] `npm run build` 통과 + `node --test` 전부 PASS.
- [ ] **lookup**: 다역본 + include_original/include_lexicon 정상.
- [ ] **search**: keyword/semantic 둘 다 동작(토큰 있으면 semantic, 없으면 폴백). 행동 판단 질의("사탕 주며 친구 데려오면 상품" 류)에서 주제 관련 구절 회수.
- [ ] **cross_references**: 관주 반환.
- [ ] **word_study**: 원어 단어→Strong's→글로스 연결.
- [ ] **로컬 모듈**: 개역개정 픽스처 투입 시 등장·기본역본 전환, 부재 시 개역한글 폴백, 본문 git 미노출.
- [ ] **SKILL**: 트리거 + 단일-synthesis 규약(다중 tool call 없음) 정적 확인.
- [ ] **NOTICE**: 출처·표기 의무 충족 + SBLGNT 제외.
- [ ] 검수 결과 PASS/FAIL 표로 보고. **Commit** `test: R3 acceptance gate green`

### Task 21: R4 — 푸시 (PM 검수 후)
- [ ] PM이 `molpass/mcp-bible` 공개 레포 생성 → remote 등록 → 로컬 커밋 push. 비밀(DEEPINFRA_TOKEN)·로컬 본문·받은 원본·DB/임베딩 git 미포함 확인. 배포 박스에서 `npm install && npm run build:data` 재현.

---

## Self-Review (스펙 대비)

- §3 UX 2종 → Task 18(SKILL) 행동판단+점진공개 + Task 12/15. ✅
- §4 도구 6종 → Task 11~16 전부. ✅
- §4 SKILL(사변형·톤·호출규약) → Task 18. ✅
- §5 데이터 스택 전 계층 → Task 4~10. SBLGNT 제외 → NOTICE Task 19. ✅
- §6 로컬 개역개정(포맷·로더·격리·기본역본) → Task 3 + 17. ✅
- §7 공통 절-ID·SQLite·FTS·임베딩·텍스트출력 → Task 2,9,10,16. (번들=빌드산출물 설계노트 1). ✅
- §8 4분할(corpus-loader/verse-id/search/skill) → File Structure 반영. ✅
- §9 오프라인 우선·시크릿 미포함·surgical·로컬커밋→검수후푸시 → .gitignore(텍스트 DB 커밋·임베딩/원본/로컬 제외) + Task 21 + 결정 1·4. ✅
- §11 검수기준 → Task 20 1:1. ✅
- **G1 교차 절-ID 정렬** → Task 8b(build-db/임베딩 직전 게이트). ✅
- **G2 PM 체크포인트** → Phase 1 종료 후 정지 마커(임베딩 이전). ✅
- **결정 1~4** → 확정 결정 섹션 + .gitignore + package.json(files/engines/scripts) + Task 10(Release 자산) + Task 17(로컬 격리) + Task 21(PM 레포·토큰). ✅

**미해결(실행 시 확정)**: 각 소스 실제 포맷(Task 4 검사 출력이 5~8 파서 입력) · `tsconfig.scripts.json`(scripts→dist-scripts) 스캐폴드 시 생성.
